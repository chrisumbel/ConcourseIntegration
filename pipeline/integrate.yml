platform: linux
    
inputs:
  - name: code

image_resource:
  type: docker-image
  source:
    repository: amidos/dcind

run:
  dir: code
  path: sh
  args:
  - -exc
  - |
    cd src/ConcourseTest
    source /docker-lib.sh
    start_docker
    docker ps
    docker-compose up